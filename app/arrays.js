if (typeof define !== 'function') { var define = require('amdefine')(module); }

define(function() {
  return {

    /**
     * @param {Array} arr
     * @param {mixed} item
     * @return number Index of item in arr.
     */
    indexOf : function(arr, item) {

    },

    /**
     * @param {Array} arr Contains numerical values.
     * @return number Sum of all values.
     */
    sum : function(arr) {

    },

    /**
     * Remove all occurences of item from the array.
     *
     * @param {Array} arr The array to be changed.
     * @param {mixed} item The value to be removed.
     * @return {Array} arr
     */
    remove : function(arr, item) {

    },

    /**
     * Create a copy of the array without "item".
     *
     * @param {Array} arr The array to be changed.
     * @param {mixed} item The value to be removed.
     * @return {Array} copy
     */
    removeWithCopy : function(arr, item) {

    },

    /**
     * @param {Array} arr
     * @param {mixed} item
     * @return {Array} arr
     */
    append : function(arr, item) {

    },

    /**
     * @param {Array} arr
     * @return {Array} arr
     */
    truncate : function(arr) {

    },

    /**
     * @param {Array} target
     * @param {Array} origin Values to add to target
     * @param {Array} target
     */
    concat : function(target, origin) {

    },

    /**
     * @param {Array} arr
     * @param {mixed} item
     * @param {number} index
     */
    insert : function(arr, item, index) {

    },

    /**
     * @param {Array} arr
     * @param {mixed} item
     * @return {number}
     */
    count : function(arr, item) {

    },

    /**
     * @param {Array} arr
     */
    duplicates : function(arr) {

    },

    /**
     * @param {Array} arr
     */
    square : function(arr) {

    },

    /**
     * @param {Array} arr
     * @param {mixed} target
     */
    findAllOccurrences : function(arr, target) {

    }
  };
});
